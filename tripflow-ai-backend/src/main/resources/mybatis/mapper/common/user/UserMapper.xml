<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tripflow.ai.common.user.dao.UserDao">

    <insert id="insertUser" parameterType="com.tripflow.ai.common.user.dto.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (is_active, last_login_at, name, email, profile_image_url, created_at, updated_at)
        VALUES (#{isActive}, #{lastLoginAt}, #{name}, #{email}, #{profileImageUrl}, now(), null)
    </insert>

    <select id="selectUserById" parameterType="long" resultType="com.tripflow.ai.common.user.dto.User">
        SELECT id, is_active, last_login_at, name, email, profile_image_url, created_at, updated_at
        FROM users
        WHERE id = #{id}
    </select>

    <select id="selectAllUsers" resultType="com.tripflow.ai.common.user.dto.User">
        SELECT id, is_active, last_login_at, name, email, profile_image_url, created_at, updated_at
        FROM users
        ORDER BY created_at DESC
    </select>

    <update id="updateUser" parameterType="com.tripflow.ai.common.user.dto.User">
        UPDATE users
        SET is_active = #{isActive},
            last_login_at = #{lastLoginAt},
            name = #{name},
            email = #{email},
            profile_image_url = #{profileImageUrl},
            updated_at = now()
        WHERE id = #{id}
    </update>

    <delete id="deleteUser" parameterType="long">
        DELETE FROM users
        WHERE id = #{id}
    </delete>

    <update id="deactivateUser" parameterType="long">
        UPDATE users
        SET is_active = false,
            updated_at = now()
        WHERE id = #{id}
    </update>

    <select id="selectUserByEmail" parameterType="string" resultType="com.tripflow.ai.common.user.dto.User">
        SELECT id, is_active, last_login_at, name, email, profile_image_url, created_at, updated_at
        FROM users
        WHERE email = #{email}
    </select>

</mapper>
