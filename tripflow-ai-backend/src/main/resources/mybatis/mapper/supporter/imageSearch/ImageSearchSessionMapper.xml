<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tripflow.ai.supporter.imageSearch.dao.ImageSearchSessionDao">
  <resultMap id="ImageSearchSessionResultMap" type="ImageSearchSession">
    <id property="id" column="id"/>
    <result property="userId" column="user_id"/>
    <result property="createdAt" column="created_at"/>
    <result property="actionType" column="action_type"/>
  </resultMap>

    <!-- INSERT -->
    <insert id="insert" parameterType="com.tripflow.ai.supporter.imageSearch.dto.response.ImageSearchSessionResponse"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO image_search_sessions (user_id, created_at, action_type)
        VALUES (#{userId}, NOW(), #{actionType}::image_action_type_enum)
    </insert>
    
    <!-- SELECT by ID -->
    <select id="selectById" resultType="com.tripflow.ai.supporter.imageSearch.dto.response.ImageSearchSessionResponse">
        SELECT id, user_id, created_at, action_type
        FROM image_search_sessions
        WHERE id = #{id}
    </select>

    <!-- SELECT by USER ID -->
    <select id="selectByUserId" resultType="com.tripflow.ai.supporter.imageSearch.dto.response.ImageSearchSessionResponse">
        SELECT id, user_id, created_at, action_type
        FROM image_search_sessions
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <!-- SELECT ALL -->
    <select id="selectAll" resultMap="ImageSearchSessionResultMap">
        SELECT *
        FROM image_search_sessions
        ORDER BY id DESC
    </select>

    <!-- UPDATE ACTION TYPE -->
    <update id="updateActionType">
        UPDATE image_search_sessions
        SET action_type = #{actionType}::image_action_type_enum
        WHERE id = #{sessionId}
    </update>

    <!-- UPDATE -->
    <update id="update" parameterType="ImageSearchSession">
        UPDATE image_search_sessions
        SET user_id = #{userId},
            created_at = #{createdAt},
            action_type = #{actionType}::image_action_type
        WHERE id = #{id}
    </update>

    <!-- DELETE -->
    <delete id="delete">
        DELETE FROM image_search_sessions
        WHERE id = #{id}
    </delete>

    <!-- FIND RECENT SESSION BY USER AND PLACES -->
    <select id="findRecentSessionByUserAndPlaces" resultType="com.tripflow.ai.supporter.imageSearch.dto.response.ImageSearchSessionResponse">
        SELECT s.id, s.user_id, s.created_at, s.action_type
        FROM image_search_sessions s
        WHERE s.user_id = #{userId}
        AND (
            SELECT array_agg(image_place_id ORDER BY image_place_id)
            FROM image_search_candidates
            WHERE image_search_session_id = s.id
        ) = (
            SELECT array_agg(unnest ORDER BY unnest)
            FROM unnest(#{placeIds}::bigint[])
        )
        ORDER BY s.created_at DESC
        LIMIT 1
    </select>
</mapper>
