<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tripflow.ai.travelgram.review.dao.ReviewPostDao">

    <!-- review_posts -->
    <insert id="insertDraft" parameterType="ReviewPost" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO review_posts (caption, is_posted, review_post_url, created_at, plan_id,
        photo_group_id, hashtag_group_id, review_style_id, overall_moods, travel_type) VALUES (null,
        false, null, NOW(), #{planId}, #{photoGroupId}, #{hashtagGroupId}, null, null, null); </insert>

    <select id="selectReviewPostById" resultType="ReviewPost"> SELECT * FROM review_posts WHERE id =
        #{id}; </select>

    <select id="selectReviewPostsByPlanId" resultType="ReviewPost"> SELECT * FROM review_posts WHERE
        plan_id = #{planId}; </select>

    <!-- 로그 식별자용: photoGroupId 하나로 reviewPostId(=id), planId 조회 -->
    <select id="selectReviewPostByPhotoGroupId" resultType="ReviewPost">
        SELECT id, plan_id AS planId, photo_group_id AS photoGroupId
        FROM review_posts
        WHERE photo_group_id = #{photoGroupId}
    </select>

    <update id="updateReviewPostGroupId"> UPDATE review_posts SET photo_group_id = #{photoGroupId},
        hashtag_group_id = #{hashtagGroupId} WHERE id = #{reviewPostId} </update>
    <update id="updateReviewPostMood"> UPDATE review_posts SET overall_moods = #{overallMoods},
        travel_type = #{travelType}::travel_type_enum WHERE photo_group_id = #{photoGroupId} </update>
    <update id="updateReviewPostStyleIdById"> UPDATE review_posts SET review_style_id =
        #{reviewStyleId} WHERE id = #{reviewPostId} </update>

    <update id="updateReviewPostCaptionIdById"> UPDATE review_posts SET caption =
        #{caption} WHERE id = #{reviewPostId} </update>

    <!-- <update id="updateReviewPost" parameterType="ReviewPost">
        UPDATE review_posts
        SET caption = #{caption}, is_posted = #{isPosted}, review_post_url = #{reviewPostUrl}, created_at =
    #{createdAt},
            plan_id = #{planId}, photo_group_id =#{photoGroupId}, hashtag_group_id=#{hashtagGroupId},
            review_style_id = #{reviewStyleId}, 
        WHERE id = #{id}
    </update> -->
</mapper>