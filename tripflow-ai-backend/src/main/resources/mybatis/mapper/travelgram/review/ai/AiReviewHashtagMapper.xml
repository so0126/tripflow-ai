<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tripflow.ai.travelgram.review.ai.dao.AiReviewDao">

  <insert id="insertAiReviewHashtag" parameterType="AiReviewHashtag" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
    INSERT INTO ai_review_hashtags (name, created_at, review_style_id)
    VALUES (#{name}, #{createdAt}, #{reviewStyleId})
</insert>

  <select id="selectHashtagsByStyleId" resultType="AiReviewHashtag">
    SELECT id, name, created_at, review_style_id
    FROM ai_review_hashtags
    WHERE review_style_id = #{reviewStyleId}
</select>
  <select id="selectAiReviewHashtagById" resultType="AiReviewHashtag">
    SELECT id, name, created_at, review_style_id
    FROM ai_review_hashtags WHERE id = #{id}
  </select>

  <update id="updateAiReviewHashtag" parameterType="AiReviewHashtag">
    UPDATE ai_review_hashtags
    SET name = #{name}, 
        created_at = #{createdAt}, 
        review_style_id = #{reviewStyleId}
    WHERE id = #{id}
  </update>

  <delete id="deleteAiReviewHashtagById" parameterType="long">
    DELETE FROM ai_review_hashtags WHERE id = #{id}
  </delete>

</mapper>