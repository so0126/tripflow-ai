<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tripflow.ai.travelgram.review.dao.ReviewPhotoDao">

  <!-- ReviewPhoto -->

  <insert id="insertReviewPhoto" parameterType="ReviewPhoto" useGeneratedKeys="true"
    keyProperty="id" keyColumn="id"> INSERT INTO review_photos ( review_post_id, file_url,
    order_index, created_at, summary ) VALUES ( #{reviewPostId}, #{fileUrl}, #{orderIndex}, now(),
    #{summary} ); </insert>

  <select id="selectReviewPhotoById" resultType="ReviewPhoto"> SELECT id, file_url, order_index, created_at, review_post_id, summary, status
    FROM review_photos WHERE id = #{id} </select>


  <select id="selectReviewPhotosByReviewPostId" resultType="ReviewPhoto">
    SELECT id, file_url, order_index, created_at, review_post_id, summary, status
    FROM review_photos
    WHERE review_post_id = #{reviewPostId}
  </select>


  <select id="selectPhotoSummariesByReviewPostId"
    resultType="string">
    SELECT summary FROM review_photos WHERE review_post_id = #{reviewPostId} AND
    summary IS NOT NULL </select>

  <update id="updatePhotoOrder"> UPDATE review_photos SET order_index = #{orderIndex} WHERE id =
    #{photoId} AND review_post_id = #{reviewPostId} </update>

  <!-- 성공 경로 전용: 요약 저장과 동시에 상태를 SUCCESS로 (한 번의 UPDATE로 원자적 갱신) -->
  <update id="updatePhotoSummary"> UPDATE review_photos SET summary = #{summary}, status = 'SUCCESS' WHERE id =
    #{photoId} </update>

  <update id="updatePhotoStatus"> UPDATE review_photos SET status = #{status} WHERE id =
    #{photoId} </update>

</mapper>
